# AnbeeApp / アンビー — HANDOFF (v3.0 統合版)

## 全体構成（v3.0で2ゲーム統合）
1つのアプリに2つのゲームが入っている。起動するとトップ画面が出て、そこで選ぶ。

| モード | クラス | 内容 |
|---|---|---|
| めいろ | `MazeView` | アンビーを指で呼び寄せてゴールへ運ぶ |
| そうこばん | `SokoView` | にもつを押してゴールにそろえる（旧 SokoApp を統合） |

- `MainActivity` が `FrameLayout` を1枚持ち、`TitleView` / `MazeView` / `SokoView` を差し替える
- **各Viewのインスタンスは使い回す**ので、トップに戻ってもう一度入っても進行状況が保たれる
- 戻る操作は フィールド左上の「≡ メニュー」ボタン と 端末の戻るキー の2通り。トップ画面で戻るキーを押すとアプリ終了
- `Chara`（アンビー描画）と `UiKit`（下部パネルUI）は両モードで共有。**ここを直すと両方に反映される**

旧 SokoApp リポジトリは統合にともない廃止した。

## 概要
迷路にちらばった10匹の「アンビー」を、指で呼び寄せながらゴールの大木まで導くパズルゲーム。
アンビーは**曲がれるのが1回だけ（＝2直線）**なので、壁に遮られると来られない。
指の位置を中継地点として少しずつリレーしていくのがコア。
ブランド: Appathy（Less Motivation, More Automation）

- package: `com.appathy.anbee`
- 表示名: アンビー
- 画面: 縦固定 / フルスクリーン / 上部がフィールド、下部がステータス・コントロールパネル

## ビルド構成（Appathy共通）
- AGP 8.5.2 / Kotlin 1.9.24 / Gradle 8.9（Actions内で直接インストール、wrapperなし）
- minSdk 26 / targetSdk 34 / compileSdk 34 / Java 17
- 外部依存ゼロ / XMLレイアウトなし（全てCanvas描画）
- `debug.keystore` はリポジトリに同梱（署名安定化のため）

## ファイル構成
```
AnbeeApp/
  build.gradle
  settings.gradle
  gradle.properties
  debug.keystore
  deploy.sh
  HANDOFF.md
  app/
    build.gradle
    src/main/AndroidManifest.xml
    src/main/java/com/appathy/anbee/MainActivity.kt   ← 画面切替
    src/main/java/com/appathy/anbee/TitleView.kt      ← トップ画面
    src/main/java/com/appathy/anbee/MazeView.kt       ← めいろ（旧 GameView.kt）
    src/main/java/com/appathy/anbee/SokoView.kt       ← そうこばん
    src/main/java/com/appathy/anbee/Levels.kt         ← そうこばんの面データ
    src/main/java/com/appathy/anbee/Chara.kt          ← アンビー描画（共有）
    src/main/java/com/appathy/anbee/UiKit.kt          ← 下部パネルUI（共有）
```

## v2.1 での変更（v2.0からの変更）
- **下部にステータス／コントロール専用パネルを新設**。フィールドと重ならないようになった
  - パネル高さ `panelH = clamp(w * 0.40, h * 0.20, h * 0.30)`。フィールド高さは `fh = h - panelH`
  - パネル上部17%がステータス行。残りに 十字キー（左）／さがす・スタート-ストップ（中央）／ミニマップ（右）
  - ステータス行の文字は `measureText` で実測して自動縮小するので、どの解像度でも収まる
- **スタート／ストップボタンを追加**（1つのボタンでトグル）
  - タイトル → スタートでゲーム開始 / プレイ中 → ストップで一時停止 / クリア後 → スタートで新しい迷路
  - 一時停止中は `update()` が早期returnし、放置タイマーも止まる
- **右下にミニマップ**。迷路全体を `Bitmap` に事前描画（`MM_K = 10` 分の1）し、毎フレーム縮小転送
  - 赤い枠＝現在の表示範囲、赤い点＝画面中心（現在地）、青い点＝アンビー、黄色い点＝ゴール
  - ミニマップをタップするとその位置へカメラが飛ぶ
- **壁を細い黒線に変更**。グリッドを不均一化し、壁と連結部だけを 40 幅にした（下記）
- 画面タップでの開始は廃止（スタートボタンに統一）。フィールドのタップは呼び寄せ専用

## v2.0 での方針転換（v1.1からの変更）
- 「群れを日光と雨から守る」ゲームから**迷路パズル**へ全面変更
- 天候（はれ／あめ）、HP、日光ダメージ、傘、鳥をすべて削除
- 傘ボタン → **「さがす」ボタン**に置き換え（遠いアンビーから順に画面を切り替え）
- 笛（解散）ボタンを削除
- アンビーは10匹固定。移動は**L字（2直線）のみ**
- 10秒放置すると、すあな（スタート地点）へ ゆっくり戻り始める

## デプロイ（deploy.sh）
`bash ~/AnbeeApp/deploy.sh "コミットメッセージ"` の1コマンドで push とタグ発行まで完結する。
第2引数に `notag` を渡すと push のみで終了する。

- トークンは `git config --global github.token` から読む。チャットに貼らない
- `git pull --rebase origin main` を必ず通す。CatalogApp が API 経由で `.github/workflows/release.yml` と `ci/appathy.keystore` を直接コミットするため、これが無いと push が rejected になる
- **`ci/` ディレクトリと `.github/workflows/release.yml` は削除しない**（`.gitignore` での追跡解除も不可）。配布ビルドに必要
- 次タグは `git fetch --tags --force` → `git tag --list 'v*' | sort -V | tail -1` の**実タグ一覧**から算出し、`git tag` → `git push origin タグ名` でローカル発行する
  - GitHub API の最新リリース基準は禁止。リリース作成が失敗するとタグだけ残り、同じ番号を再計算して弾かれ、push は通っているのに Actions が走らない事故になる
  - API の `git/ref/heads/main` 参照も禁止。push 直後は反映待ちで一つ前のコミットにタグが付く
  - タグが1つも無いときは awk が空を返すので `v1.0.0` へフォールバックする（この分岐は消さないこと）
- タグが打たれると Actions がビルドして Release を作り、自作アプリストアに更新として現れる
- リポジトリ自体の作成は行わない。未作成の場合は先に API で作っておくこと
- v2.1 まで `GameView.kt` だったファイルは `MazeView.kt` に改名したため、deploy.sh に `rm -f app/src/main/java/com/appathy/anbee/GameView.kt` を入れてある。これが無いと端末に旧ファイルが残り、`GameView` と `MazeView` の二重定義でコンパイルが落ちる
- v2.1.2 まで同梱していた `.github/workflows/build.yml` を削除したため、deploy.sh に `rm -f .github/workflows/build.yml` を入れてある。`unzip -o` は端末側の旧ファイルを消さないので、この行が無いと復活してしまう

### CI は release.yml のみ
**`build.yml` は今後いっさい同梱しない。**
`actions/upload-artifact` を使うと Artifacts 無料枠（0.5GB）が枯渇し、"Artifact storage quota has been hit" で全ビルドが落ちる。
APK は タグ → `release.yml` → Release の経路でのみ配布する。

## めいろ（MazeView）の仕様

### 迷路（v2.1で不均一グリッド化）
- グリッド 27 x 41。**列と行の幅が奇数/偶数で異なる**
  - 奇数インデックス = 部屋。幅 `FW = 150`
  - 偶数インデックス = 壁／連結部。幅 `WT = 40`
- これにより壁が細い黒線になり、通路が太くなる。`colX[] / rowY[]` に累積座標を持ち、`cellX() / cellY()` は線形走査で逆引き
- ワールドサイズ 2510 x 3840
- (偶数, 偶数) は柱で常に壁。(奇数, 奇数) は必ず部屋。片方だけ奇数のセルが連結部
- 生成は再帰的バックトラッカー（奇数マス掘り）
- 生成後、**連結部のみ**を確率 `LOOP_P = 0.22` で追加開放 → ループを作る。柱は絶対に開けない（開けると 40x40 の穴ができて破綻する）
- スタート `(1, ROWS-2)` 左下 / ゴール `(COLS-2, 1)` 右上。両方とも上下左右の連結部を開放
- 起動ごとにランダム生成。スタートボタンで新しい迷路

### アンビーの移動ルール（コア）
指の位置のセルを `T`、アンビーのいるセルを `A` として、
1. `A` から横一直線に進み、角で縦に曲がって `T` へ
2. `A` から縦一直線に進み、角で横に曲がって `T` へ

このどちらかが**全マス床**なら「とどく」＝寄ってくる。両方とも壁に遮られていれば来られない。
距離の長い軸を先に試す。再計算は 0.12 秒ごと（指を動かすと即時）。

- とどくアンビーには薄い黄色のL字ガイド線が引かれる
- とどかないアンビーは色が褪せ、口がへの字になる
- 指を離すとその場で停止（元の位置には戻らない）

### 詰み防止の検証済み事項
L字移動のみを辺とするグラフで、**全床マスからゴールへ到達可能**であることを25通りの生成結果で確認済み（`LOOP_P` = 0.16 / 0.22 / 0.28 / 0.34 のいずれでも詰みゼロ）。
迷路サイズや `LOOP_P` を変えるときは、この性質が保たれるか必ず再検証すること。

### 放置と帰巣
- アンビーごとに `idle` タイマー。指に「とどいている」間は 0 にリセット
- `idle > 10秒` で `BACK` モード。スタート地点からのBFS距離マップ `dist[]` を下る形で迷路を正しく戻る
- 帰巣速度 96（呼び寄せ速度 400 に対してかなり遅い）
- 6秒経過で頭上に「?」、帰巣開始で「＜」が出る

### 状態
`mode` は3つ: `STAY`(その場) / `COME`(指へ移動中) / `BACK`(すあなへ帰る)
加えて `saved` フラグ。ゴールのセルに入ると `saved = true` で固定。10匹全員でクリア。

### 操作
| 操作 | 割り当て |
|---|---|
| フィールドを指で押す | その位置へアンビーを呼ぶ。押している間だけ有効 |
| 指をドラッグ | 呼び先が動く。ゆっくり動かして迷路を誘導する |
| 十字キー（左下） | カメラスクロール |
| さがす（十字キーの上） | ゴールから遠い順にアンビーへカメラを移動。押すたび次の個体へ。対象に赤いリング |
| スタート/ストップ | 1つのボタン。開始 / 一時停止 / 再開 / クリア後は新しい迷路 |
| ミニマップをタップ | その位置へカメラを移動 |
| 戻るキー | 終了 |

### ステータス表示
- 指を押している間: `ついてきてる N / とどかない N`
- 常時: `ゴール N/10`、帰巣中がいれば `すあなへ もどってる N`

### 主要パラメータ
| 定数 | 値 | 意味 |
|---|---|---|
| `COLS / ROWS` | 27 / 41 | 迷路サイズ |
| `FW` | 150 | 部屋の幅 |
| `WT` | 40 | 壁・連結部の幅 |
| `LOOP_P` | 0.22 | ループ用の連結部開放率。詰み防止に効く |
| `MM_K` | 10 | ミニマップの縮小率 |
| `TOTAL` | 10 | アンビー数 |
| `COME_SPEED` | 400 | 呼び寄せ移動速度 |
| `BACK_SPEED` | 96 | 帰巣速度 |
| `CAM_SPEED` | 900 | 十字キーのスクロール速度 |
| `IDLE_LIMIT` | 10 | 帰巣開始までの秒数 |
| `REPLAN` | 0.12 | L字経路の再計算間隔（秒） |

## 実装メモ
- マルチタッチはポインタIDで管理（`fid` 指 / `did` 十字キー）。さがすボタンは押下即発火でID保持なし
- `onDown` はまず `y >= fh` でパネルかフィールドかを分岐。パネル内は 十字キー → さがす → スタート/ストップ → ミニマップ の順
- セル内オフセットは `fitOff()` でそのセルの幅に応じてクランプする。連結部（幅40）では自動的に0になり、細い通路の中央を通る
- アンビーは個体ごとに `ox, oy`（セル内の固定オフセット、半径 `CS*0.26`）を持ち、団子にならないようにしている
- 加えて `separate()` で軽い反発。壁にめり込まないよう軸ごとに `isFree()` で判定してから適用
- 初期配置はスタートからのBFS距離順に並べ、22%〜85%の位置に散らす
- 描画は可視セル範囲のみループ
- ゴールが画面外のときは方向ガイド矢印を表示

## 今後の候補
- ステージ制（迷路サイズと曲がれる回数の段階的変化）
- 「曲がれる回数」をアイテムで一時的に2回にする
- 動く壁 / 開閉する扉
- クリアタイムのローカル記録
- 手数（指を置き直した回数）の評価

## そうこばん（SokoView）
旧 SokoApp v1.0 を移植したもの。詳細は下記。

### 面データ（Levels.kt）
標準の倉庫番テキスト形式。`#` かべ / スペース ゆか / `.` ゴール / `$` にもつ / `*` ゴール上のにもつ / `@` アンビー / `+` ゴール上のアンビー

現在10面。**全面をソルバ（BFS + 隅デッドロック枝刈り）で可解性検証済み。**

| レベル | 盤面 | にもつ | 最短手数 |
|---|---|---|---|
| 1 | 7x5 | 1 | 1 |
| 2 | 9x6 | 2 | 4 |
| 3 | 8x6 | 2 | 13 |
| 4 | 9x7 | 2 | 18 |
| 5 | 8x7 | 3 | 12 |
| 6 | 9x7 | 3 | 14 |
| 7 | 9x7 | 3 | 18 |
| 8 | 10x7 | 4 | 12 |
| 9 | 10x8 | 4 | 46 |
| 10 | 10x8 | 3 | 51 |

**面を追加・変更したら必ずソルバで再検証すること。** 手で作った面は解けないことが多い（初版で作った1面が壁で分断されていて解なしだった）。
検証は「箱数とゴール数の一致」「初期配置がデッドマスにないこと」「BFSで解が見つかること」「行の長さが揃っていること」の4点。

### 操作
| 操作 | 割り当て |
|---|---|
| 十字キー | 1歩移動。0.30秒押しっぱなしで 0.11秒ごとに連続移動 |
| 盤面をスワイプ | 1マス分ずらすごとに1歩。指を離さず連続で動かせる |
| もどす | 1手アンドゥ。履歴は無制限 |
| やりなおし | 現在の面を初期状態に戻す |
| 右のレベル番号グリッド | タップでその面へ |
| クリア後に盤面タップ | 次の面へ（最終面なら1面へ） |

### 実装メモ
- 盤面は `cell: ByteArray`（WALL=1 / GOAL=2 のビット）と `boxes: ArrayList<Box>` の分離。にもつはマスに埋めない
- スクロールなし。`fitBoard()` が画面に収まる倍率 `cs` を計算して中央寄せ
- 表示位置はマス座標へ毎フレーム補間（LERP=20）。論理座標は即時更新なので入力を取りこぼさない
- アンドゥは `Move`（移動前のプレイヤー座標、押したにもつのindexと移動前座標）を積むだけ。盤面スナップショットは取らない
- クリア済みは `SharedPreferences("soko")` に "1011..." 形式で永続化。起動時は未クリアの最初の面を開く

## 2ゲームの今後の合流案
現状は「同じアプリの別モード」でしかない。ゲームとして混ぜるなら以下が候補。

- そうこばんの面に複数のアンビーを置き、めいろと同じ「呼び寄せ」で押させる
- めいろの途中に、にもつを押して道を作る仕掛けを混ぜる
- 共通のワールドマップから各ステージへ入る構成にする

技術的な障壁として、めいろは不均一グリッド（部屋150 / 壁40）＋スクロール、そうこばんは均一グリッド＋全体フィットで座標系が違う。混ぜるなら均一グリッド側に寄せるのが素直。
