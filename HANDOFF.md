# AnbeeApp / アンビー — HANDOFF

## 概要
指の影で小さな生き物（アンビー）の群れを守り、ゴールの大木まで全員を運ぶスマホ専用ゲーム。
「指が画面を覆って見えない」という欠点をそのままメカニクスにしている。
ブランド: Appathy（Less Motivation, More Automation）

- package: `com.appathy.anbee`
- 表示名: アンビー
- 画面: 縦固定 / フルスクリーン / 上65%がフィールド、下35%がファミコン風パッド

## ビルド構成（Appathy共通）
- AGP 8.5.2 / Kotlin 1.9.24 / Gradle 8.9（Actions内で直接インストール、wrapperなし）
- minSdk 26 / targetSdk 34 / compileSdk 34 / Java 17
- 外部依存ゼロ / XMLレイアウトなし（全てCanvas描画）
- `debug.keystore` はリポジトリに同梱（署名安定化のため）

## ファイル構成
```
AnbeeApp/
  build.gradle
  settings.gradle
  gradle.properties
  debug.keystore
  HANDOFF.md
  .github/workflows/build.yml
  app/
    build.gradle
    src/main/AndroidManifest.xml
    src/main/java/com/appathy/anbee/MainActivity.kt
    src/main/java/com/appathy/anbee/GameView.kt   ← ゲーム全体
```

## ゲーム仕様

### ワールド
- ワールドサイズ 2400 x 3600（画面より広い）
- スタート地点（すあな）: (340, 3220) 半径175の日陰あり
- ゴール（大木）: (1980, 470) 半径235の日陰
- アンビー総数 12

### 操作
| 操作 | 割り当て |
|---|---|
| フィールドを指で押す | 影を作る。群れが指の下と周りに集まる |
| 指をドラッグ | 影ごと群れを移動 |
| 十字キー | カメラ（画面）スクロール。移動ではない |
| かいさん | 群れを一斉に散らす。影の中心付近の個体は0.6秒の逃げ遅れ |
| かさ | 押している間だけ展開。雨のみ有効・雨のみゲージ消費 |
| スタート | ゲーム開始 / クリア後リトライ |
| しゅうりょう（セレクト相当） | アプリ終了 |

### 状態
アンビーの状態は4つ: `FOLLOW`(追従) / `LOST`(はぐれ) / `SAVED`(ゴール済) / `DOWN`(すあな送り)

- 指を離すと、影の外にいた個体は `LOST` に散る（影の中の個体はその場に残る）
- `LOST` は指の影に0.85倍半径まで近づくと再合流（散った直後1.7秒はロック）
- HPが0になると `DOWN` になりスタート地点のすあなへ戻される。すあなは日陰なので安全
- `DOWN` 個体は指の影で触れると `FOLLOW` に復帰（HP 0.5）→ ゴール後に拾いに戻る周回プレイが成立
- ゴールの木の内側0.8倍半径に入ると `SAVED`。12匹全員 `SAVED` でクリア

### ダメージ
| 天候 | 危険 | 安全条件 | 減少/秒 |
|---|---|---|---|
| はれ | 日光 | 指の影 / 大木 / すあな | 0.105 |
| あめ | 雨 | 傘 / 大木 / すあな | 0.165 |

回復 0.075/秒。HPで見た目のサイズが変わる（半径 4.5 + 10 * hp）。

### 傘
- 半径 = 影半径 x 1.55
- ゲージ消費 0.135/秒（雨天かつ展開中のみ）、非展開時に 0.055/秒 回復
- 晴れでは日光を防がない（日光は影、雨は傘、と役割を分離）

### 巨大生物
- 13〜22秒間隔。`FOLLOW`/`LOST` の重心を狙う
- 予兆2.6秒（暗い円が降下 + 赤いリング）→ 踏みつけ（半径260）
- ダメージ 0.35 + 0.75 x 中心度。生存個体は吹き飛ばされて `LOST`
- 散らしておけば被害減、散らさなければ群れごと直撃

### 天候サイクル
- はれ 15〜23秒 → あめ 9〜14秒 の繰り返し

## 実装メモ
- 日光表現は `saveLayer` + `PorterDuff.Mode.CLEAR` で暖色オーバーレイに影の穴を開けている
- マルチタッチはポインタIDで管理（`fid` 指 / `did` 十字キー / `uid` 傘）
- 描画ループは `onDraw` 内で dt 計算 → `postInvalidateOnAnimation()`
- ゴールが画面外のときは HUD に方向ガイド矢印を出す

## 今後の候補
- ステージ制（複数の木、川、風で影が流れる等）
- はぐれ個体の鳴き声インジケータ（画面外の方向表示）
- クリアタイムのローカル記録
- アンビーの種類追加（速い個体 / 弱い個体）
