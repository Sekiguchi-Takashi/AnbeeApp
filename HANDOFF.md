# AnbeeApp / アンビー — HANDOFF (v1.1)

## 概要
指の影で小さな生き物「アンビー」の群れを守り、ゴールの大木まで全員を運ぶスマホ専用ゲーム。
「指が画面を覆って見えない」という欠点をそのままメカニクスにしている。
ブランド: Appathy（Less Motivation, More Automation）

- package: `com.appathy.anbee`
- 表示名: アンビー
- 画面: 縦固定 / フルスクリーン / フィールドは全画面、コントロールは左下に半透明オーバーレイ

## ビルド構成（Appathy共通）
- AGP 8.5.2 / Kotlin 1.9.24 / Gradle 8.9（Actions内で直接インストール、wrapperなし）
- minSdk 26 / targetSdk 34 / compileSdk 34 / Java 17
- 外部依存ゼロ / XMLレイアウトなし（全てCanvas描画）
- `debug.keystore` はリポジトリに同梱（署名安定化のため）

## ファイル構成
```
AnbeeApp/
  build.gradle
  settings.gradle
  gradle.properties
  debug.keystore
  HANDOFF.md
  .github/workflows/build.yml
  app/
    build.gradle
    src/main/AndroidManifest.xml
    src/main/java/com/appathy/anbee/MainActivity.kt
    src/main/java/com/appathy/anbee/GameView.kt   ← ゲーム全体
```

## v1.1 での変更点
- キャラクターを大型化し、青いたまねぎ型のからだ＋黄色い花のデザインに刷新（`drawAnt`）
- 下部のコントロールパッド帯を廃止し、フィールドを全画面化
- 十字キーを左下に配置、その上に「傘」ボタン、さらに上に「笛」（解散）ボタンの縦一列
- スタート／終了ボタンを廃止。タイトルとクリア画面は画面タップで開始・リトライ。終了は端末の戻るキー
- 巨大生物を「鳥」に変更。上空の旋回が予兆になり、そのあと急降下してつつく
- 笛で散らしたときの移動距離を短くし、必ず画面内にとどまるようクランプ

## ゲーム仕様

### ワールド
- ワールドサイズ 2400 x 3600（画面より広い）
- スタート地点（すあな）: (340, 3220) 半径230の日陰あり
- ゴール（大木）: (1980, 470) 半径300の日陰
- アンビー総数 12

### 画面依存パラメータ（`m = min(w, h)`）
| 値 | 定義 |
|---|---|
| 影の半径 `shadeR` | `m * 0.24` |
| 傘の半径 `umbR` | `shadeR * 1.55` |
| キャラ基準サイズ `antR` | `m * 0.030` |
| 隊列の距離 | `shadeR * (0.32 + (i % 3) * 0.19)` |

### 操作
| 操作 | 割り当て |
|---|---|
| フィールドを指で押す | 影を作る。群れが指の下と周りに集まる |
| 指をドラッグ | 影ごと群れを移動 |
| 十字キー（左下） | カメラ（画面）スクロール。移動ではない |
| 笛（いちばん上） | 群れを一斉に散らす。影の中心付近の個体は0.6秒の逃げ遅れ |
| 傘（まんなか） | 押している間だけ展開。雨のみ有効・雨のみゲージ消費。残量はボタン外周のリング |
| 画面タップ（タイトル / クリア時） | 開始・リトライ |
| 戻るキー | 終了 |

### 状態
アンビーの状態は4つ: `FOLLOW`(追従) / `LOST`(はぐれ) / `SAVED`(ゴール済) / `DOWN`(すあな送り)

- 指を離すと、影の外にいた個体は `LOST` に散る（影の中の個体はその場に残る）
- `LOST` は散らばり先 `(tx, ty)` へ移動してその場をうろつく。散らばり先は必ず画面内にクランプされる
- `LOST` は指の影に0.85倍半径まで近づくと再合流（散った直後1.6秒はロック、頭上に「!」表示）
- HPが0になると `DOWN` になりスタート地点のすあなへ戻される。すあなは日陰なので安全
- `DOWN` 個体は指の影で触れると `FOLLOW` に復帰（HP 0.5）→ ゴール後に拾いに戻る周回プレイが成立
- ゴールの木の内側0.75倍半径に入ると `SAVED`。12匹全員 `SAVED` でクリア

### ダメージ
| 天候 | 危険 | 安全条件 | 減少/秒 |
|---|---|---|---|
| はれ | 日光 | 指の影 / 大木 / すあな | 0.105 |
| あめ | 雨 | 傘 / 大木 / すあな | 0.165 |

回復 0.075/秒。HPで見た目のサイズ（`antR * (0.5 + 0.5 * hp)`）、からだの色、花の大きさ、口の形が変わる。

### 傘
- 半径 = 影半径 x 1.55
- ゲージ消費 0.135/秒（雨天かつ展開中のみ）、非展開時に 0.055/秒 回復
- 晴れでは日光を防がない（日光は影、雨は傘、と役割を分離）

### 鳥（`bSt`）
| 状態 | 内容 |
|---|---|
| `B_NONE` | 13〜22秒の待機 |
| `B_CIRCLE` | 4.2秒。群れの上空を旋回。地面に鳥の影が走り、赤い危険円（半径300）を表示。これが予兆 |
| `B_DIVE` | 0.7秒。高度を落として急降下。着地時に `strike()` |
| `B_LEAVE` | 1.3秒。上昇して離脱。着弾リングを描画 |

- `strike()` のダメージ: `0.30 + 0.80 * 中心度`（半径 `HIT_R = 300`）
- 生存個体は吹き飛ばされて `LOST`
- 旋回中に笛で散らしておけば被害減、散らさなければ群れごと直撃

### 天候サイクル
- はれ 15〜23秒 → あめ 9〜14秒 の繰り返し

## 実装メモ
- 日光表現は `saveLayer` + `PorterDuff.Mode.CLEAR` で暖色オーバーレイに影の穴を開けている
- マルチタッチはポインタIDで管理（`fid` 指 / `did` 十字キー / `uid` 傘）。笛は押した瞬間に発火するのでID保持なし
- `onDown` の判定順は 十字キー → 傘 → 笛 → フィールド
- 描画ループは `onDraw` 内で dt 計算 → `postInvalidateOnAnimation()`
- 群れの重心は `flockCenter()` が `ctrX/ctrY` に書き込む。鳥の狙点 `tgx/tgy` とは分離してあるので、急降下中に笛を押しても狙点はずれない
- ゴールが画面外のときは方向ガイド矢印を表示

## 今後の候補
- ステージ制（複数の木、川、風で影が流れる等）
- はぐれ個体の方向インジケータ
- クリアタイムのローカル記録
- アンビーの色ちがい（速い個体 / 弱い個体）
- 鳥以外の脅威（地面から出る、夜になる等）
